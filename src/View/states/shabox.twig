<article class="menu-target8">
    <h3>Shabox</h3>

    <section class="card">
        <h4>Shabox</h4>
        <p>You need to add a state keyword to use the Shabox classes...</p>
        <a class="btn" href="https://github.com/animadio/animadio/blob/master/src/states/shabox.css">View Shabox Source Code</a>
    </section>

    <table class="table">
        <caption>Shabox Datas</caption>

        <thead>
        <tr>
            <th>Class</th>
            <th>Property</th>
            <th>Value</th>
            <th>Screen</th>
            <th>Concat</th>
        </tr>
        </thead>

        <tbody>
        {% for shabox in allShaboxClasses %}
            <tr>
                <th>{{ shabox.class }}</th>
                <td>{{ shabox.property }}</td>
                <td>{{ shabox.valor }}</td>
                <td>{{ shabox.screen }}</td>
                <td>{{ shabox.concat|replace({'1': "yes", '0': "no"}) }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</article>
