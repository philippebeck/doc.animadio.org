<article class="menu-target1">
    <h3>Anima</h3>

    <section class="card-blue">
        <h4>Anima</h4>
        <p>You need to add a state keyword to use the Anima classes...</p>
        <a class="btn-sky" href="https://github.com/animadio/animadio/blob/master/src/states/anima.css">View Anima Source Code</a>
    </section>

    <table id="anima-datas" class="table">
        <caption>Anima Datas</caption>

        <thead>
        <tr>
            <th>Class</th>
            <th>Property</th>
            <th><a href="{{ url('global') }}" title="How to use Variables ?">Variable</a></th>
            <th>@media</th>
            <th><a href="{{ url('grid') }}" title="How to Concat ?">Concat</a></th>
            <th><a href="{{ url('states') }}" title="How States work ?">States</a></th>
        </tr>
        </thead>

        <tbody>
        {% for anima in allAnimaClasses %}
            <tr>
                <th>{{ anima.class }}</th>
                <td>{{ anima.property }}</td>
                <td>{{ anima.variable }}</td>
                <td>{{ anima.screen }}</td>
                <td>{{ anima.concat|replace({'1': "yes", '0': "no"}) }}</td>
                <td>{{ anima.state|replace({'1': "yes", '0': "no"}) }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</article>
